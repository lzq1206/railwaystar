{
  "name": "railwaystar",
  "version": "1.0.0",
  "private": true,
  "description": "China Railway Dark Sky Map data pipeline and static frontend",
  "scripts": {
    "generate:data": "node scripts/generate_data.js",
    "build": "npm run generate:data",
    "start": "python -m http.server 8000"
  },
  "dependencies": {
    "axios": "^1.9.0"
  }
}
